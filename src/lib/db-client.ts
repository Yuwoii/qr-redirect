/**
 * Direct export of repositories for module resolution issues
 * This file provides direct exports of repositories to avoid issues with module resolution
 */

import { userRepository, qrCodeRepository, redirectRepository } from './db';

export { userRepository, qrCodeRepository, redirectRepository }; 